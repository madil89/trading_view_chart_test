import React from 'react';
import ReactDOM from 'react-dom/client';
import FXChartContainer from './Chart/FXChartContainer';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <FXChartContainer />
  </React.StrictMode>
);
